export const environment = {
  production: false,
  apiGatewayUrl: 'http://localhost:8956',
  // When true the frontend uses mock participations in MesResultats by default
  mockParticipations: true
};
